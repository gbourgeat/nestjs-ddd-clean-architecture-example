# Git Branch Strategy - Visual Guide

## 🌳 Vue d'ensemble de la stratégie de branches

```
main (protected)
 │
 │   feature/RS-123-add-endpoint
 ├─────────────────────────────────────┐
 │                                     │
 │  ┌─ commit: feat(api): add endpoint│
 │  │                                  │
 │  ├─ commit: test: add tests         │
 │  │                                  │
 │  └─ commit: docs: update docs       │
 │                                     │
 ├─────────── [PR MERGE] ─────────────┘
 │
 │   fix/RS-456-validation-bug
 ├─────────────────────────────────────┐
 │                                     │
 │  ┌─ commit: fix(api): validation    │
 │  │                                  │
 │  └─ commit: test: add test          │
 │                                     │
 ├─────────── [PR MERGE] ─────────────┘
 │
 ▼
```

## 📊 Cycle de vie d'une feature

```
┌──────────────────────────────────────────────────────────────┐
│  1. Créer la branche                                         │
│     git checkout -b feature/RS-123-description               │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  2. Développer                                               │
│     • Coder                                                  │
│     • Tester                                                 │
│     • Commiter                                               │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  3. Pousser                                                  │
│     git push -u origin feature/RS-123-description            │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  4. Créer Pull Request                                       │
│     • Remplir le template                                    │
│     • Assigner reviewers                                     │
│     • Ajouter labels                                         │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  5. Review & Corrections                                     │
│     • Review du code                                         │
│     • Commentaires                                           │
│     • Corrections                                            │
│     • Re-review                                              │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  6. CI/CD Checks                                             │
│     ✅ Build                                                 │
│     ✅ Tests (unit, integration, e2e)                        │
│     ✅ Linting                                               │
│     ✅ Coverage                                              │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  7. Merge                                                    │
│     • Squash and merge (recommandé)                          │
│     • Supprimer la branche                                   │
└────────────────────┬─────────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────────┐
│  8. Cleanup                                                  │
│     git checkout main                                        │
│     git pull origin main                                     │
│     git branch -d feature/RS-123-description                 │
└──────────────────────────────────────────────────────────────┘
```

## 🎯 Règles de protection de `main`

```
┌─────────────────────────────────────────────────────────────┐
│                      Branch: main                           │
│                     (PROTECTED)                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ❌ Push direct                                             │
│  ❌ Force push                                              │
│  ❌ Deletion                                                │
│                                                             │
│  ✅ Merge via Pull Request uniquement                       │
│  ✅ Requiert 1+ approbation                                 │
│  ✅ CI/CD doit passer                                        │
│  ✅ Branche à jour avec base                                │
│  ✅ Tous les commentaires résolus                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Types de branches et leur utilisation

```
┌──────────────────────────────────────────────────────────────┐
│ feature/   │ Nouvelles fonctionnalités                       │
│            │ Ex: feature/RS-123-add-endpoint                 │
│            │ Merge: main ← feature/                          │
├────────────┼──────────────────────────────────────────────────┤
│ fix/       │ Corrections de bugs                             │
│            │ Ex: fix/RS-456-validation-error                 │
│            │ Merge: main ← fix/                              │
├────────────┼──────────────────────────────────────────────────┤
│ hotfix/    │ Corrections urgentes                            │
│            │ Ex: hotfix/RS-789-security-patch                │
│            │ Merge: main ← hotfix/ (prioritaire)             │
├────────────┼──────────────────────────────────────────────────┤
│ refactor/  │ Refactoring sans changement fonctionnel         │
│            │ Ex: refactor/RS-234-extract-service             │
│            │ Merge: main ← refactor/                         │
├────────────┼──────────────────────────────────────────────────┤
│ docs/      │ Documentation uniquement                        │
│            │ Ex: docs/RS-567-api-documentation               │
│            │ Merge: main ← docs/                             │
├────────────┼──────────────────────────────────────────────────┤
│ test/      │ Ajout/modification de tests                     │
│            │ Ex: test/RS-890-e2e-coverage                    │
│            │ Merge: main ← test/                             │
├────────────┼──────────────────────────────────────────────────┤
│ chore/     │ Maintenance, dépendances                        │
│            │ Ex: chore/RS-345-update-deps                    │
│            │ Merge: main ← chore/                            │
├────────────┼──────────────────────────────────────────────────┤
│ perf/      │ Optimisations de performance                    │
│            │ Ex: perf/RS-678-optimize-algorithm              │
│            │ Merge: main ← perf/                             │
└────────────┴──────────────────────────────────────────────────┘
```

## 📝 Anatomie d'un bon commit

```
┌─────────────────────────────────────────────────────────────┐
│ feat(api): add POST /road-segments endpoint                 │  ← Type & Description (50 chars max)
│                                                             │
│ Implement complete endpoint to create road segments.       │  ← Body (72 chars per line)
│                                                             │
│ Features:                                                   │
│ - Request/Response DTOs with validation                     │
│ - Controller with error handling                            │
│ - Use case orchestration                                    │
│ - 12 unit tests + 15 E2E tests                              │
│                                                             │
│ Follows hexagonal architecture principles.                  │
│                                                             │
│ Closes #123                                                 │  ← Footer (référence issue)
└─────────────────────────────────────────────────────────────┘

Types:
  feat     ✨  Nouvelle fonctionnalité
  fix      🐛  Correction de bug
  docs     📝  Documentation
  style    💄  Formatage
  refactor ♻️  Refactoring
  perf     ⚡️  Performance
  test     ✅  Tests
  build    🔧  Build system
  ci       👷  CI/CD
  chore    🔨  Maintenance

Scopes (optionnel):
  api, domain, infra, db, tests, docs, deps
```

## 🔀 Stratégies de merge

### Squash and Merge (Recommandé)

```
feature/RS-123-add-endpoint
├─ commit 1: WIP: start implementation
├─ commit 2: add validation
├─ commit 3: fix typo
├─ commit 4: add tests
└─ commit 5: address review comments

         ⬇️  SQUASH

main
└─ feat(api): add POST /road-segments endpoint
```

**Avantages:**
- ✅ Historique propre et lisible
- ✅ Un commit = une feature
- ✅ Facile à revert
- ✅ Pas de commits "WIP"

### Rebase and Merge

```
feature/RS-123-add-endpoint
├─ commit 1: feat(api): add endpoint structure
├─ commit 2: feat(api): add validation
└─ commit 3: test(api): add comprehensive tests

         ⬇️  REBASE

main
├─ feat(api): add endpoint structure
├─ feat(api): add validation
└─ test(api): add comprehensive tests
```

**Avantages:**
- ✅ Garde l'historique détaillé
- ✅ Historique linéaire
- ✅ Bons pour les grosses features

## 🚦 Workflow des Pull Requests

```
┌────────────────────────────────────────────────────────────┐
│  PR Créée                                                  │
│  [FEATURE] Add POST /road-segments endpoint                │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│  Checks Automatiques                                       │
│  ⏳ Build                                                   │
│  ⏳ Tests                                                   │
│  ⏳ Linting                                                 │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│  Review Demandée                                           │
│  👤 Reviewer 1 assigné                                     │
│  👤 Reviewer 2 assigné (optionnel)                         │
└────────────────┬───────────────────────────────────────────┘
                 │
        ┌────────┴────────┐
        ▼                 ▼
┌──────────────┐   ┌──────────────┐
│ ✅ Approved  │   │ 🔄 Changes   │
│              │   │    Requested │
└──────┬───────┘   └──────┬───────┘
       │                  │
       │                  ▼
       │           ┌──────────────┐
       │           │ Fix & Push   │
       │           └──────┬───────┘
       │                  │
       └──────────┬───────┘
                  ▼
┌────────────────────────────────────────────────────────────┐
│  Prêt à Merger                                             │
│  ✅ Approuvé                                                │
│  ✅ CI passe                                                │
│  ✅ Branche à jour                                          │
│  ✅ Commentaires résolus                                    │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│  Merge                                                     │
│  🎉 Squash and merge                                        │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│  Cleanup Automatique                                       │
│  🗑️  Branche supprimée                                      │
└────────────────────────────────────────────────────────────┘
```

## 🎯 Checklist avant de créer une PR

```
✅ Le code compile sans erreurs
✅ Tous les tests passent (unit, integration, e2e)
✅ La couverture de tests est maintenue/améliorée
✅ Le code est linté (npm run lint)
✅ La documentation est à jour
✅ Les commits suivent Conventional Commits
✅ La branche est à jour avec main
✅ J'ai testé manuellement
✅ Pas de console.log ou code de debug
✅ Pas de secrets/credentials
```

## 💡 Tips & Best Practices

### Garder les PRs petites

```
❌ BAD PR (1200 lignes)
- Ajoute 5 endpoints
- Refactor de 10 fichiers
- Update de la documentation
- Fix de 3 bugs

✅ GOOD PR (250 lignes)
- Ajoute 1 endpoint
- Tests associés
- Documentation de l'endpoint
```

### Commits atomiques

```
❌ BAD
git commit -m "fix stuff"

✅ GOOD
git commit -m "fix(api): handle empty city names in validation

Validate city names before creating road segments to prevent
database errors.

Fixes #456"
```

### Review constructive

```
❌ BAD
"This code is bad"

✅ GOOD
"Consider extracting this validation logic into a separate
service for better reusability. What do you think?"
```

## 📊 Métriques de qualité

```
PR idéale:
├─ Taille: < 500 lignes
├─ Temps de review: < 2 jours
├─ Commentaires: 0-5
├─ Tests: 100% des nouvelles lignes
├─ Commits: 3-5 (avant squash)
└─ Time to merge: < 3 jours
```
